public class DosandDonts {
    public static void main(String[] args)
    {
        //only extends is allowed in inheriting the class and implementing the interface
        //extends from only one class and multiple interfaces e.g. class MyArray<T extends A & B & C> where A is class and B and C are interfaces
        //wild Cards <?> will accept but cannot access
        
    }
}
